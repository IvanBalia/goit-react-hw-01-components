import styled from "styled-components";

export const Avatar = styled.img`
display:block;
width:80px;
border-radius:50%;
margin-top: ${({theme})=>theme.space[1]};
margin-left:auto;
margin-right:auto;
`